import org.gradle.api.Plugin
import org.gradle.api.Project

class CourseConventionsPlugin : Plugin<Project> {
    override fun apply(project: Project) {
        // Convention plugin - actual plugins are applied in individual build files
    }
}
